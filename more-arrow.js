console.log('This is the more-arrow.js file');


// 20% discount ===
//20/100
const getTax = (amount, taxRate) => amount * taxRate / 100;

// single parameter 

const getSquare = (x) => x * x
const result = getSquare(5);
console.log(result);

const getHalf = number => number / 2
const half = getHalf(10)
console.log(half)